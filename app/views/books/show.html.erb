  <% if flash[:notice] %>
      <div class= "flash">
        <%= flash[:notice] %>
        <%= flash[:alert] %>
      </div>
  <% end %>

  <%= render 'users/userinfo', user: current_user %>
  <%= render 'books/newbook', book: @new_book %>

  <div class= "container">
  <div class= "row">

<div class= "col-xs-9">
<h1>Books detail</h1>
<table class="table table-hover table-inverse">
　<tr>
　　<th></th>
　　<th></th>
   <th></th>
   <th></th>
   <th></th>
　</tr>

   <tr>
   <td><%= attachment_image_tag @book.user, :profile_image, :fill, 60, 60, format: 'jpeg', class: "img-circle pull-left profile-img", fallback: "no_image.jpg" %>
     <%= link_to @book.user.name, user_path(@book.user.id), class: "user_#{@book.user.id}" %>
   </td>
   <td><%= link_to @book.title, book_path(@book), class: "book_#{@book.user_id}" %></td>
   <td><%= @book.body %></td>
   <td></td>
  <% if @book.user == current_user %>
  <td><%= link_to "Edit", edit_book_path(@book.id), class: "edit_book_#{@book.id}" %></td>
   <td><%= link_to "Destroy", book_path(@book.id), class: "destroy_book_#{@book.id}", method: :delete, "data-confirm" => "本当に削除しますか？" %></td>
   <% end %>
   </tr>
</table>
</div>
</div>